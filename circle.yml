## Customize the test machine
machine:
  python:
    version: pypy-2.2.1

test:
  post:
    - ./test.py
dependencies:
  pre:
    - pip install PIL --allow-external PIL --allow-unverified PIL
deployment:
  production:
    branch: master
    commands:
      - fab deploy

